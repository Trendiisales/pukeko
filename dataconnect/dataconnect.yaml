specVersion: "v1"
serviceId: "fresh-tailwind"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fresh-tailwind-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
