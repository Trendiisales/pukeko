// Initialize Firebase
const firebaseConfig = {
  projectId: 'pukeko-social',
  // Add other config properties if you have them
};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);
console.log('Firebase initialized successfully');
